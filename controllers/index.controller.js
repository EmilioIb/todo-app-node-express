const projectsController = require('./projects.controller');
const todosController = require('./todos.controller');
const timesController = require('./times.controller');
const reportsController = require('./reports.controller');

module.exports = {
  projectsController,
  todosController,
  timesController,
  reportsController,
};
