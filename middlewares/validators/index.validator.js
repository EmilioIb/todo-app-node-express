const projectsValidator = require('./projects.validator');
const todosValidator = require('./todos.validator');
const timesValidator = require('./times.validator');

module.exports = {
  projectsValidator,
  todosValidator,
  timesValidator,
};
